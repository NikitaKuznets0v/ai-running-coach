# N8N Workflow Конфигурации

Эта папка содержит JSON конфигурации для всех N8N workflow проекта AI Running Coach.

## 📁 Файлы

### `workflow-main-chatbot.json`
Основной чат-бот для общения с пользователями:
- Получает сообщения через Telegram Trigger
- Определяет стадию пользователя (onboarding/active)
- Обрабатывает через AI (OpenAI GPT)
- Сохраняет историю в БД
- Отправляет ответ в Telegram

### `workflow-plan-generator.json`
Генератор недельных планов тренировок:
- Триггер: команда `/plan` или "Составь план"
- Извлекает данные пользователя из БД
- Генерирует план через AI
- Сохраняет plan в БД
- Форматирует и отправляет в Telegram

### `workflow-training-logger.json`
Логирование выполненных тренировок:
- Определяет сообщения о тренировках
- Извлекает данные через AI (NLP)
- Сохраняет в таблицу trainings
- Обновляет статус в weekly_plans
- Дает обратную связь

## 🔧 Импорт в N8N

### Через UI:
1. Откройте N8N
2. Workflows → Import from File
3. Выберите JSON файл
4. Настройте credentials
5. Activate workflow

### Через API:
```bash
curl -X POST https://your-instance.app.n8n.cloud/api/v1/workflows \
  -H "X-N8N-API-KEY: your_api_key" \
  -H "Content-Type: application/json" \
  -d @workflow-main-chatbot.json
```

## 📝 Примечания

- Все workflow требуют настройки credentials (Telegram, OpenAI, Supabase)
- JSON файлы используются как backup и для миграции между серверами
- Версионируются в git для отслеживания изменений
