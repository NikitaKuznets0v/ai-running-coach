# Database Schema и Миграции

SQL схема и миграции для проекта AI Running Coach (PostgreSQL через Supabase).

## 📁 Файлы

### `schema.sql`
Полная схема базы данных:
- Таблица `users` - профили пользователей
- Таблица `weekly_plans` - недельные планы тренировок
- Таблица `trainings` - журнал тренировок
- Таблица `chat_history` - история диалогов

### `migrations/`
Инкрементальные миграции для обновления схемы БД.

### `seeds/`
Тестовые данные для локальной разработки.

## 🚀 Первоначальная Настройка

1. Откройте Supabase Dashboard
2. SQL Editor → New query
3. Скопируйте содержимое `schema.sql`
4. Run
5. Проверьте Table Editor - все таблицы созданы

## 📝 Примечания

- БД использует UUID для primary keys
- Все timestamps в UTC
- Foreign keys настроены с CASCADE для безопасного удаления
- Индексы настроены для частых запросов
